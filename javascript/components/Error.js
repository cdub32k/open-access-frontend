import React from "react";

const Error = (props) => {
  return (
    <div style={{ textAlign: "center" }}>
      Not found. This item does not exist or could have been deleted
    </div>
  );
};

export default Error;
